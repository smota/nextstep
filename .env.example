# Optional when commands run from inside the private vault; otherwise set an absolute path.
NEXTSTEP_DATA_ROOT=

# Optional overrides. Normally leave these unset.
# NEXTSTEP_STATE_ROOT=
HOLOSELF_EXECUTABLE=
